const concatenate = (str1, str2) => str1 + " " + str2;
const capitalize = (str) => str.charAt(0).toUpperCase() + str.slice(1);
const fullname = "Daniel";

module.exports = { concatenate, capitalize, fullname };
